---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gameservices.gs.zzrr.io
spec:
  group: gs.zzrr.io
  names:
    kind: GameService
    listKind: GameServiceList
    plural: gameservices
    singular: gameservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.deployGroup.role
      name: Role
      type: string
    - jsonPath: .spec.deployGroup.active
      name: Active
      type: boolean
    - jsonPath: .status.connectorCount
      name: Pods
      type: integer
    - jsonPath: .status.connectorImage
      name: Version
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              connectorNamespace:
                minLength: 1
                type: string
              deployGroup:
                properties:
                  active:
                    type: boolean
                  role:
                    enum:
                    - blue
                    - green
                    type: string
                required:
                - active
                - role
                type: object
              extraIngress:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  name:
                    minLength: 1
                    type: string
                  paths:
                    items:
                      properties:
                        path:
                          minLength: 1
                          type: string
                        pathType:
                          enum:
                          - Prefix
                          - Exact
                          - ImplementationSpecific
                          type: string
                        port:
                          format: int32
                          maximum: 65535
                          minimum: 1
                          type: integer
                        serviceName:
                          minLength: 1
                          type: string
                      required:
                      - path
                      - pathType
                      - port
                      - serviceName
                      type: object
                    minItems: 1
                    type: array
                required:
                - name
                - paths
                type: object
              gateway:
                properties:
                  parentRef:
                    properties:
                      name:
                        minLength: 1
                        type: string
                      namespace:
                        minLength: 1
                        type: string
                      sectionName:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                required:
                - parentRef
                type: object
              ingress:
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  ingressClassName:
                    minLength: 1
                    type: string
                  tls:
                    properties:
                      secretName:
                        minLength: 1
                        type: string
                    required:
                    - secretName
                    type: object
                required:
                - ingressClassName
                type: object
              podLabelKey:
                minLength: 1
                type: string
              podLabelValue:
                minLength: 1
                type: string
              retention:
                properties:
                  defaultDuration:
                    pattern: ^[1-9][0-9]*h$
                    type: string
                  enabled:
                    type: boolean
                required:
                - defaultDuration
                - enabled
                type: object
              route:
                properties:
                  host:
                    minLength: 1
                    pattern: ^[a-zA-Z0-9]([a-zA-Z0-9\-\.]*[a-zA-Z0-9])?$
                    type: string
                  pathPrefix:
                    minLength: 1
                    type: string
                  pathType:
                    enum:
                    - Prefix
                    - Exact
                    - ImplementationSpecific
                    type: string
                  port:
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                required:
                - host
                - pathPrefix
                - pathType
                - port
                type: object
              trafficMode:
                default: Ingress
                enum:
                - Ingress
                - Gateway
                type: string
            required:
            - connectorNamespace
            - deployGroup
            - podLabelKey
            - podLabelValue
            - route
            type: object
            x-kubernetes-validations:
            - message: ingress is required when trafficMode is Ingress
              rule: has(self.trafficMode) && self.trafficMode == 'Gateway' || has(self.ingress)
            - message: gateway is required when trafficMode is Gateway
              rule: '!has(self.trafficMode) || self.trafficMode != ''Gateway'' ||
                has(self.gateway)'
          status:
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectorCount:
                format: int32
                type: integer
              connectorImage:
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
